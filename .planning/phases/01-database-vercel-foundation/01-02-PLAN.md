---
phase: 01-database-vercel-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .env.local
autonomous: false
requirements:
  - INFRA-04
  - INFRA-05
user_setup:
  - service: neon
    why: "Database connection for quiz data storage"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection Details -> Connection string (with password visible). Use the same Neon project as glow-up. The quiz tables use quiz_ prefix so no collision."
    dashboard_config: []
  - service: vercel
    why: "Hosting for static files + API routes"
    env_vars:
      - name: DATABASE_URL
        source: "Same Neon connection string — set in Vercel project settings -> Environment Variables"
      - name: DASHBOARD_TOKEN
        source: "Generate any random string (e.g. run: openssl rand -hex 16). Set in Vercel project settings -> Environment Variables"
    dashboard_config:
      - task: "Link burnout-quiz repo to a Vercel project"
        location: "Vercel Dashboard -> Add New Project -> Import Git Repository -> select burnout-quiz"
must_haves:
  truths:
    - "drizzle-kit push creates quiz_leads and quiz_events tables in Neon"
    - "DATABASE_URL is configured locally in .env.local for drizzle-kit"
    - "DATABASE_URL and DASHBOARD_TOKEN are configured in Vercel environment variables"
    - "Visiting the Vercel deployment URL shows the existing quiz HTML"
  artifacts:
    - path: ".env.local"
      provides: "Local DATABASE_URL for drizzle-kit push"
      contains: "DATABASE_URL"
  key_links:
    - from: "drizzle.config.ts"
      to: ".env.local"
      via: "dotenv/config loads DATABASE_URL"
      pattern: "import.*dotenv/config"
    - from: "api/_lib/db.ts"
      to: "Neon PostgreSQL"
      via: "process.env.DATABASE_URL"
      pattern: "process.env.DATABASE_URL"
---

<objective>
Push the Drizzle schema to Neon PostgreSQL and verify the full deployment works — static HTML served and database tables created.

Purpose: Complete the infrastructure foundation by ensuring the database is live and Vercel deployment serves everything correctly.
Output: Live quiz_leads and quiz_events tables in Neon, working Vercel deployment, environment variables configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-vercel-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Created by Plan 01 — executor needs these to verify -->

From api/_lib/schema.ts:
```typescript
export const quizLeads = pgTable('quiz_leads', { ... })
export const quizEvents = pgTable('quiz_events', { ... })
export type QuizLead = InferSelectModel<typeof quizLeads>
export type NewQuizLead = InferInsertModel<typeof quizLeads>
export type QuizEvent = InferSelectModel<typeof quizEvents>
export type NewQuizEvent = InferInsertModel<typeof quizEvents>
```

From drizzle.config.ts:
```typescript
import 'dotenv/config'
export default {
  schema: './api/_lib/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: { url: process.env.DATABASE_URL! },
} satisfies Config
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure environment variables</name>
  <what-built>Plan 01 created all project files: schema, db connection, health check, Vercel config. Now we need the actual DATABASE_URL to push the schema.</what-built>
  <how-to-verify>
The executor needs the user to provide the Neon DATABASE_URL. Steps:

1. **Get the Neon connection string:**
   - Go to Neon Console (https://console.neon.tech)
   - Open the project shared with glow-up
   - Go to Connection Details
   - Copy the connection string (make sure password is visible)
   - Format: `postgresql://user:password@host/dbname?sslmode=require`

2. **The executor will create `.env.local`** with the provided DATABASE_URL

3. **Set Vercel environment variables:**
   - Go to Vercel Dashboard -> burnout-quiz project -> Settings -> Environment Variables
   - Add `DATABASE_URL` = (same Neon connection string)
   - Add `DASHBOARD_TOKEN` = (run `openssl rand -hex 16` to generate one)
   - Apply to all environments (Production, Preview, Development)

4. **Ensure the Vercel project is linked to the burnout-quiz GitHub repo** so pushes trigger deployments.
  </how-to-verify>
  <resume-signal>Provide the DATABASE_URL connection string (or confirm .env.local is already created with it). Confirm Vercel env vars are set.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push schema to Neon and verify deployment</name>
  <files>.env.local</files>
  <read_first>
    - .env.local (confirm DATABASE_URL is set)
    - drizzle.config.ts (confirm schema path and dialect)
    - api/_lib/schema.ts (confirm table definitions)
  </read_first>
  <action>
1. Verify `.env.local` exists and contains DATABASE_URL:
```bash
grep -q 'DATABASE_URL' .env.local
```

2. Run `npx drizzle-kit push` to create the quiz_leads and quiz_events tables in Neon.
   - This command reads `drizzle.config.ts` which loads `dotenv/config` for `.env.local`
   - It will create both tables with the `quiz_` prefix in the Neon database
   - If it prompts for confirmation, it should auto-accept in non-interactive mode
   - Expected output: tables created successfully or "nothing to push" if already done

3. Verify the tables were created by running a quick check:
```bash
npx drizzle-kit push
```
If drizzle-kit push requires interactive confirmation that cannot be suppressed, create the `.env.local` with the DATABASE_URL and run the command. If it truly blocks on TTY, flag for manual intervention.

4. Run TypeScript type check to ensure everything still compiles:
```bash
npx tsc --noEmit
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local` contains a line starting with `DATABASE_URL=postgresql://`
    - `npx drizzle-kit push` completes without errors
    - `npx tsc --noEmit` passes with zero errors
    - Tables `quiz_leads` and `quiz_events` exist in Neon (confirmed by push output)
  </acceptance_criteria>
  <done>drizzle-kit push successfully created quiz_leads and quiz_events tables in Neon PostgreSQL, TypeScript compiles cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Vercel deployment serves quiz</name>
  <what-built>
Complete infrastructure: Neon database with quiz_leads and quiz_events tables, Vercel project configured with static HTML + API routes, environment variables set.
  </what-built>
  <how-to-verify>
1. Push to the main branch (or trigger a Vercel deployment)
2. Visit the Vercel deployment URL (e.g., `https://burnout-quiz-xxx.vercel.app/`)
3. Verify the quiz loads and works exactly as before (intro screen, start quiz, answer questions)
4. Visit `https://burnout-quiz-xxx.vercel.app/api/health` and verify it returns JSON: `{"status":"ok","timestamp":"..."}`
5. If the health check returns `{"status":"error"}`, the DATABASE_URL env var is not set in Vercel — go to Settings -> Environment Variables and verify

Expected: Quiz works identically to before. Health endpoint confirms DB connection.
  </how-to-verify>
  <resume-signal>Type "approved" if quiz loads and health check returns ok, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local → git | DATABASE_URL must stay gitignored |
| Vercel env vars → source code | Secrets only in env vars, never hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | .env.local committed | mitigate | `.gitignore` from Plan 01 excludes `.env.local` — verify before any git commit |
| T-01-04 | Information Disclosure | DASHBOARD_TOKEN weak | mitigate | Generated via `openssl rand -hex 16` (128-bit entropy) |
| T-01-05 | Denial of Service | /api/health exposes DB status | accept | Health endpoint is low-risk; returns generic error, no DB details leaked |
</threat_model>

<verification>
1. `npx drizzle-kit push` succeeds — tables created in Neon
2. `npx tsc --noEmit` passes
3. Vercel deployment URL serves quiz HTML
4. `/api/health` returns `{"status":"ok"}`
5. `.env.local` is gitignored (not in `git status`)
</verification>

<success_criteria>
- quiz_leads and quiz_events tables exist in Neon PostgreSQL
- DATABASE_URL configured in .env.local (local) and Vercel (production)
- DASHBOARD_TOKEN configured in Vercel
- Visiting Vercel URL shows the working burnout quiz
- GET /api/health returns 200 with status ok and timestamp
- Existing quiz functionality is completely unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-vercel-foundation/01-02-SUMMARY.md`
</output>
